VITE_APP_NAME=
VITE_WALLET_PROJECT_ID=
VITE_DZAP_API_KEY=
# Overrides the SDK's default zap API base URL (e.g. http://localhost:8081 for a local backend).
# Also used by the few calls that bypass the SDK (limit orders); the `/v1` suffix is optional.
ZAP_API_URL=
